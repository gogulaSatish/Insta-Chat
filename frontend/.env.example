# Frontend Environment Variables for Vite

# Socket URL - for development, defaults to current window origin
# For production on Render, the socket will connect via window.location.origin
VITE_SOCKET_URL=http://localhost:5000

# API Base URL if needed for future use
VITE_API_URL=http://localhost:5000
